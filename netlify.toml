# Déploiement Netlify — build Nitro ciblant Netlify (SSR + routes /api).
[build]
  command = "npm run build:netlify"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--legacy-peer-deps"
  NITRO_PRESET = "netlify"
